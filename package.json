{
  "name": "@f/css-unitless",
  "description": "List of unitless css properties",
  "repository": "git://github.com/micro-js/css-unitless.git",
  "version": "1.0.2",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/hyphenate": "^1.0.0"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
